MSGRAPH_MCP_CLIENT_ID=
MSGRAPH_MCP_TENANT_ID=
# Optional: override token cache location (default ~/.msgraph-mcp/token_cache.bin)
# MSGRAPH_MCP_TOKEN_CACHE_PATH=
# Legacy OUTLOOK_MCP_* names are still honored as a fallback.
